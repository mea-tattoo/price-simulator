name: Round segment slightly

on:
  push:
    branches:
      - segment-border-refine
    paths:
      - '.github/workflows/round-segment-slightly.yml'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: segment-border-refine
      - name: Adjust segmented control corners
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          old='.segment{display:grid;grid-template-columns:1fr 1fr;gap:.7px;max-width:732px;margin:auto;padding:.7px;background:#171717;border:2px solid #262626;border-radius:4px;box-shadow:0 1px 2px rgba(0,0,0,.08)}\n.segment button{border:0;border-radius:3.3px;background:transparent;color:#bdbdbd;font:inherit;font-size:14px;font-weight:700;padding:10px 8px;cursor:pointer}'
          new='.segment{display:grid;grid-template-columns:1fr 1fr;gap:.7px;max-width:732px;margin:auto;padding:.7px;background:#171717;border:2px solid #262626;border-radius:6px;box-shadow:0 1px 2px rgba(0,0,0,.08)}\n.segment button{border:0;border-radius:5px;background:transparent;color:#bdbdbd;font:inherit;font-size:14px;font-weight:700;padding:10px 8px;cursor:pointer}'
          if old not in s:
              raise SystemExit('segment CSS target not found')
          p.write_text(s.replace(old,new,1),encoding='utf-8')
          PY
      - name: Commit change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git diff --cached --quiet && exit 0
          git commit -m "Slightly round segmented control"
          git push origin segment-border-refine
